-r requirements.txt
pytest>=9.0.0
pytest-asyncio>=0.24.0  # kept as optional dep; tests use anyio (@pytest.mark.anyio)
anyio[trio]>=4.0.0      # provides pytest.mark.anyio and trio backend
httpx>=0.27.0
